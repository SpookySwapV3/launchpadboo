{
  "name": "launchpadboo",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ launchpadboo",
    "create-local": "graph create --node http://localhost:8020/ launchpadboo",
    "remove-local": "graph remove --node http://localhost:8020/ launchpadboo",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 launchpadboo",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.90.1",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
